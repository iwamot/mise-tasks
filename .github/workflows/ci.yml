name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    uses: iwamot/workflows/.github/workflows/ci.yml@fb6b31c58b26c1baaa822c1a2798d00e5fddef27 # v0.7.0
