{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "github>iwamot/renovate-config#v0.10.0",
    "docker:disable"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Track aqua tools declared in mise file tasks (#MISE tools={...})",
      "managerFilePatterns": [
        "/(^|/)\\.mise/tasks/[^/]+$/"
      ],
      "matchStrings": [
        "\"aqua:(?<depName>[^\"]+)\"\\s*=\\s*\"(?<currentValue>[^\"]+)\""
      ],
      "datasourceTemplate": "github-releases",
      "extractVersionTemplate": "^v?(?<version>.+)$"
    },
    {
      "customType": "regex",
      "description": "Track pipx tools declared in mise file tasks (#MISE tools={...})",
      "managerFilePatterns": [
        "/(^|/)\\.mise/tasks/[^/]+$/"
      ],
      "matchStrings": [
        "\"pipx:(?<depName>[^\"]+)\"\\s*=\\s*\\{[^}]*version\\s*=\\s*\"(?<currentValue>[^\"]+)\""
      ],
      "datasourceTemplate": "pypi"
    },
    {
      "customType": "regex",
      "description": "Track npm tools declared in mise file tasks (#MISE tools={...})",
      "managerFilePatterns": [
        "/(^|/)\\.mise/tasks/[^/]+$/"
      ],
      "matchStrings": [
        "\"npm:(?<depName>[^\"]+)\"\\s*=\\s*\\{[^}]*version\\s*=\\s*\"(?<currentValue>[^\"]+)\""
      ],
      "datasourceTemplate": "npm"
    }
  ]
}
